# Spring
spring.thymeleaf.cache=false
server.port=58913

# Internal UI only
management.endpoints.enabled-by-default=false

# Optional media cache directory for application.yml / application.properties.
# Defaults to a "media" directory next to the running jar when unset.
# ts3audiobot.media.dir=D:/ts3audiobot-media
